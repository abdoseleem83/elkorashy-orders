# فحص شامل — تطبيق «القرشي | طلبات الموزعين»

> تاريخ الفحص: ١١ أغسطس ٢٠٢٦ · النسخة المفحوصة: `v173`
> نطاق الفحص: `index.html` (7518 سطر) + `apps_script.gs` (1204 سطر) + `sw.js` + `manifest.json`

---

## ملخص سريع

| البند | التقييم |
|---|---|
| المنتج ووظائفه | ⭐⭐⭐⭐⭐ ممتاز — تغطية شغل حقيقية ومدروسة جدًا |
| تجربة الاستخدام | ⭐⭐⭐⭐⭐ مبهرة، خصوصًا لجمهور غير تقني |
| **الأمان** | ⭐ **ضعيف جدًا — فيه تسريب بيانات فعلي شغال دلوقتي** |
| صحة الوظائف | ⭐⭐⭐ فيه ٣ مميزات مكسورة تمامًا وشغالة "بالشكل" بس |
| قابلية الصيانة | ⭐⭐ ملف واحد ٦٦٧ كيلوبايت، من غير build ولا اختبارات |
| الأداء والتوسع | ⭐⭐ هيقف عند ~٢٠٠٠–٣٠٠٠ طلب |

**الخلاصة:** التطبيق ده من ناحية *المنتج* شغل محترم جدًا وواضح إن ورا كل تفصيلة فيه تجربة حقيقية من الشغل على الأرض. المشكلة مش في الأفكار — المشكلة في **٥ حاجات حرجة** لازم تتصلح قبل أي حاجة تانية، وأخطرهم واحدة بتخلي أي حد على الإنترنت يقدر يسحب كل طلباتك بأسماء وأرقام تليفونات عملائك في ١٠ نداءات.

---

# القسم الأول: المميزات ✅

## ١. تغطية وظيفية شاملة جدًا

التطبيق مش مجرد "فورم طلب" — ده نظام كامل:

**للموزّع:**
- تسجيل حساب جديد بموافقة الأدمن، ودخول بيوزر/باسورد.
- **دخول بالبصمة** (WebAuthn) بعد أول تسجيل دخول — زي تطبيقات البنوك بالظبط.
- تصفح الأصناف بالقطاعات والتصنيفات الفرعية، بحث عام وبحث داخل القطاع.
- اختيار ألوان متعددة للصنف الواحد بكميات ووحدات مختلفة لكل لون.
- إضافة "أصناف أخرى" مش موجودة في الكتالوج.
- مؤشرات الرصيد بـ٣ مستويات (متاح / كمية قليلة / مفيش) مع تحديد اللون بالظبط.
- **حفظ تلقائي للمسودة** واستكمالها لو التطبيق اتقفل.
- تعديل طلب متبعت (بينشئ نسخة جديدة والقديمة بتتعلّم «مُعدّل»).
- تتبع الطلبات، ومعاينة/تحميل/مشاركة عرض السعر PDF وصورة الطلب.
- إشعارات واتساب جاهزة بضغطة واحدة.

**للأدمن (متابعة المخزن):**
- قايمة الطلبات بفلاتر وحالات، تغيير حالة الطلب، حذف، أرشفة.
- إشعار بطلبات جديدة مع صوت تنبيه (polling).
- تقرير أرصدة PDF، تصدير Excel، صور طلبية النواقص لكل مخزن.
- موافقة/رفض طلبات الباسورد.

**لتبويب الإدارة (PIN منفصل):**
- تعديل وإضافة أصناف من جوه التطبيق (بدل فتح الشيت).
- رفع Excel جماعي (كل الأعمدة أو الرصيد بس) + تصدير كامل + تنزيل قالب.
- موافقة/رفض طلبات تسجيل الحسابات الجديدة.

## ٢. معالجة أخطاء ناضجة ومبنية على تجارب حقيقية

ده أكتر حاجة لفتت نظري. الكود مليان معالجات لمشاكل واقعية حصلت فعلاً:

- **`saveOrderRemote` (سطر 2148):** لو الطلب اتأخر (timeout)، مابيقولش "فشل" على طول — بيستنى ٢.٥ ثانية، وبيسأل السيرفر «الطلب ده اتسجل ولا لأ؟» قبل ما يبلّغ المستخدم. ده تفكير هندسي ممتاز.
- **`doPostInner_` (سطر 154):** حماية **idempotent** — لو نفس رقم الطلب وصل مرتين (المستخدم ضغط مرتين أو النت أعاد المحاولة)، السيرفر بيرجّع رقم الطلب الموجود بدل ما يضيف صف تاني.
- **`loadOrders` (سطر 2209):** لو فشل التحديث، **مابيمسحش** القايمة الظاهرة — بيسيبها ويقول "التحديث فشل". ده بيمنع رعب "الطلبات اختفت".
- **`rowToOrder` (سطر 619):** صف واحد فيه JSON باظ مابيوقّعش كل الطلبات — بيرجع بأصناف فاضية والباقي يظهر عادي.
- **`setup()` (سطر 33):** فيه تحذير مكتوب إن النسخة القديمة كان فيها `sheet.clear()` واتفقدت بيه طلبات فعلاً — والدالة اتحوّلت لـ"آمنة" مابتمسحش حاجة.
- **`asText()` (سطر 1745):** جوجل شيتس بيرجّع التليفون كرقم وبيشيل الصفر الأول — والكود بيحوّله لنص عشان `.trim()` ماتقعش.
- **طابور الواتساب:** CallMeBot بطيء وكان بيأخّر رد السيرفر لدرجة إن التطبيق يعتبر الحفظ فشل. الحل: طابور في شيت + trigger كل دقيقة. حل صح تمامًا.

## ٣. حماية ممتازة ضد XSS

في **٢١٠ استخدام** لدالة `esc()` عبر الملف. فحصت كل الأماكن اللي بيدخل فيها نص من المستخدم أو من الشيت جوه HTML — **مالقيتش ولا حالة واحدة غير محمية**. ده انضباط نادر في كود مكتوب بالإيد من غير framework.

## ٤. حلول PWA ذكية

- **`sw.js` بيتخطى كاش الـ CDN بتاع GitHub Pages** بإضافة `?_v=Date.now()` للرابط (سطر 106) — ده حل صح لمشكلة حقيقية إن Fastly بيفضل يرجّع نسخة قديمة حتى مع `cache: 'no-store'`.
- Network-first للـ HTML، stale-while-revalidate لباقي الملفات.
- `skipWaiting()` + `clients.claim()` + `client.navigate()` — التحديث بيوصل فورًا من غير ما المستخدم يعمل حاجة.
- كشف iPadOS اللي بيقول عن نفسه "Macintosh" عن طريق `maxTouchPoints` (سطر 6160).
- تفرقة بين Safari وغيره على iOS، لأن "إضافة للشاشة الرئيسية" مابتشتغلش من كروم/واتساب.

## ٥. تفاصيل UX مدروسة

- الحفاظ على مكان التمرير (scroll) بين عمليات الرسم — فيه ٦ دوال مخصصة لده (`captureUiState_`, `restoreUiState_`, `withPositionAnchor_`...).
- قفل ارتفاع `<body>` أثناء استبدال `innerHTML` عشان الصفحة ماتقفزش (سطر 6661).
- تجهيز الصور **في الخلفية** فور الحفظ، عشان لما المستخدم يدوس "ابعت واتساب" تكون `navigator.share()` جوه ضغطة حقيقية — لأن المتصفح بيرفض المشاركة بعد `await` طويل. تشخيص دقيق جدًا.
- تحديد محتوى خانة الأرقام تلقائيًا عند اللمس، عشان الرقم الجديد يمسح القديم بدل ما يتلزق جنبه.
- تعليقات الكود كلها بالعربي، وكل تعليق بيشرح **ليه** مش **إيه** — ده أسلوب توثيق ممتاز.

## ٦. رقم طلب مستقل لكل مخزن

`getNextOrderNumber_` (سطر 304) بيستخدم `LockService` عشان لو طلبين وصلوا في نفس اللحظة مايخدوش نفس الرقم. صح ومهم.

---

# القسم الثاني: العيوب والأخطاء ❌

## 🔴 حرج — لازم يتصلح النهاردة

### ح-١. أي حد على الإنترنت يقدر يسحب كل طلباتك (تسريب بيانات فعلي)

**المكان:** `apps_script.gs` سطر 1095–1114

```javascript
if (action === 'lookup') {
  // ← مفيش أي تحقق من التوكن هنا خالص
  ...
  const phone = normalizePhone(e.parameter.phone);
  orders = orders.filter(o => {
    const stored = normalizePhone(o.distPhone);
    if (!stored || !phone) return false;
    return stored === phone || stored.endsWith(phone) || phone.endsWith(stored);
    //                          ^^^^^^^^^^^^^^^^^^^^^ دي المصيبة
  });
}
```

**المشكلة:** المطابقة بـ `endsWith` مالهاش حد أدنى لطول الرقم. يعني:

```
?action=lookup&phone=1   ← بيرجّع كل طلب رقم تليفونه بينتهي بـ 1  (~١٠٪ من الطلبات)
?action=lookup&phone=2   ← والباقي...
...
?action=lookup&phone=0
```

**عشر نداءات بس = كل الطلبات في النظام.** بأسماء الموزعين، والمناطق، وأرقام التليفونات، والأصناف، والكميات، والملاحظات. من غير أي رقم سري ولا توكن ولا حاجة.

ورابط الـ `/exec` نفسه مكتوب في `index.html` سطر 1299، والملف عام على GitHub. يعني المدخل ده متاح لأي حد بيفتح كود الصفحة.

**ملحوظة إضافية:** نفس البند ده بيسبب مشكلة وظيفية كمان — الموزع اللي رقمه `01001234561` ممكن يشوف طلبات موزع تاني رقمه بينتهي بنفس الأرقام.

**الحل المطلوب:**
1. اشتراط طول أدنى للرقم (٩ أرقام على الأقل) وإلغاء `endsWith` تمامًا — مطابقة كاملة بس.
2. الأفضل: ربط `lookup` بجلسة المستخدم — الموزع يشوف طلباته هو بس، عن طريق توكن بيتصدر عند تسجيل الدخول (زي توكن الأدمن بالظبط، الآلية موجودة أصلاً في الكود).

---

### ح-٢. كل الأسرار مكتوبة في مستودع عام

**المكان:** `apps_script.gs`

| السطر | السر | الخطورة |
|---|---|---|
| 651 | `setAdminPin()` → `'3184'` | 🔴 رقم الأدمن كامل |
| 694 · 702 | `setManagePin()` → `'311984'` | 🔴 رقم الإدارة كامل |
| 16 | `VAPID_PRIVATE_KEY` | 🔴 مفتاح خاص للإشعارات |
| 31 | `CALLMEBOT_APIKEY = '3318176'` | 🟠 مفتاح واتساب |
| 30 | `ADMIN_WHATSAPP` | 🟡 رقم تليفون |

**المفارقة:** الكود نفسه فيه تعليق في سطر 641 بيقول بالنص:

> «⚠️ قبل كده كان رقم الأدمن مكتوب جوه index.html — والملف ده عام على GitHub، يعني أي حد يفتح كود الصفحة كان يشوف الرقم»

الرقم اتشال من `index.html` فعلاً ✅ … بس اتحط في `apps_script.gs` اللي **في نفس المستودع العام بالظبط**. الحماية اتلغت من غير ما حد ياخد باله.

يعني أي حد يفتح `apps_script.gs` على GitHub دلوقتي يقدر يدخل «متابعة المخزن» بالرقم `3184` — ومنها يحذف طلبات، ويغيّر حالات، ويوافق على تغيير باسوردات، ويرفع كتالوج كامل.

**الحل المطلوب:**
1. **غيّر الرقمين فورًا** (`3184` و `311984`) واعتبرهم مكشوفين.
2. **جدّد الـ VAPID keys** وجدّد مفتاح CallMeBot.
3. شيل القيم من الكود واستخدم `PropertiesService` بس:
   ```javascript
   function setAdminPin() {
     // الرقم بيتكتب في محرر Apps Script وقت التشغيل، مش في الكود
     const pin = Browser.inputBox('اكتب رقم الأدمن الجديد');
     PropertiesService.getScriptProperties().setProperty('ADMIN_PIN', pin);
   }
   ```
4. **مهم:** تغيير الملف مش بيمسح التاريخ — الأسرار هتفضل موجودة في الكوميتات القديمة. لازم الأرقام تتغير فعليًا، مش تتشال من الملف وخلاص.
5. `getManagePin_()` سطر 702 بيحط الرقم تلقائيًا لو مش موجود — شيل السطر ده خالص.

---

### ح-٣. كلمات المرور مخزّنة نص صريح

**المكان:** `apps_script.gs` سطر 344, 368, 878

الباسوردات بتتخزن في شيت `المستخدمين` عمود B كنص عادي، والمقارنة بتتم بـ `String(rows[i][1]) !== String(password)` مباشرة.

وأسوأ: في شيت `طلبات الباسورد` (سطر 224)، الباسورد الجديد بيتخزن **نص صريح كمان** وبيفضل موجود في الشيت للأبد حتى بعد الموافقة.

**كمان:** الدخول بيتم عن طريق **GET**:
```
?action=login&username=xxx&password=yyy
```
يعني الباسورد بيتكتب في **سجلات Google Apps Script** وفي تاريخ المتصفح.

**ومفيش أي حد لمحاولات الدخول** — مافيش قفل حساب ولا تأخير، فالتخمين الآلي مفتوح تمامًا.

**الحل المطلوب:**
1. تخزين hash بدل النص الصريح:
   ```javascript
   function hashPw_(pw, salt) {
     const raw = Utilities.computeDigest(
       Utilities.DigestAlgorithm.SHA_256, salt + '|' + pw, Utilities.Charset.UTF_8);
     return raw.map(b => ((b & 0xFF) + 0x100).toString(16).slice(1)).join('');
   }
   ```
   مع salt عشوائي لكل مستخدم في عمود إضافي.
2. تحويل `login` من GET لـ POST.
3. عدّاد محاولات فاشلة لكل يوزر (٥ محاولات → قفل ١٥ دقيقة) في `PropertiesService`.
4. مسح عمود الباسورد من شيت طلبات الباسورد بعد الموافقة.

---

### ح-٤. أي حد يقدر يعطّل إشعارات أي مستخدم

**المكان:** `apps_script.gs` سطر 83–87

```javascript
if (data.action === 'savePushSub') {
  const ok = savePushSub_(String(data.username||''), data.sub||null);
  // ← مفيش تحقق من إن اللي بيبعت هو صاحب الحساب فعلاً
```

أي حد يبعت `{action:'savePushSub', username:'أي_حد', sub:null}` بيمسح اشتراك الإشعارات بتاع المستخدم ده، أو يوجّهه لجهاز تاني.

**الحل:** اشتراط توكن جلسة المستخدم (نفس الحل المقترح في ح-١).

---

### ح-٥. العمليات المدمِّرة كلها بـ GET

**المكان:** `apps_script.gs` سطر 1029, 1074, 1085

`updateStatus`, `delete`, `deleteDelivered` كلهم بيتنفذوا على `doGet`. يعني مجرد فتح رابط في المتصفح بيمسح طلب. `deleteDelivered` بيمسح **كل** الطلبات المنفَّذة دفعة واحدة بنداء GET واحد.

**الحل:** نقلهم لـ `doPost`، وإضافة تأكيد على السيرفر لعمليات الحذف الجماعي.

---

## 🟠 مميزات مكسورة (موجودة في الواجهة، مش شغالة فعليًا)

### م-١. الأرشيف مكسور بالكامل — ٣ نداءات مش موجودة في السيرفر

قارنت كل `action` بتتنادى من الواجهة بكل `action` معرّفة في الباك إند. **٣ نداءات ناقصة تمامًا:**

| النداء | مكانه في الواجهة | موجود في السيرفر؟ |
|---|---|---|
| `archiveDone` | `index.html:2304` | ❌ **لأ** |
| `archivedOrders` | `index.html:2321` | ❌ **لأ** |
| `restoreArchived` | `index.html:2335` | ❌ **لأ** |

**اللي بيحصل فعليًا:** النداء بيوصل السيرفر، مايلاقيش `if` مطابق، فيقع لآخر الدالة (سطر 1195) اللي بترجّع **قايمة كل الطلبات**. والنتيجة:

- **زرار «أرشفة الطلبات المنفَّذة»:** بيرجع array، والكود بيدوّر على `d.ok` اللي مش موجود → «حصل خطأ في الأرشفة». مافيش حاجة اتأرشفت.
- **شاشة «الأرشيف»:** `Array.isArray(data)` بترجع `true`! فـ `state.archivedOrders` بيتحط فيها **كل الطلبات النشطة**. يعني **الأرشيف بيعرض كل الطلبات على إنها مؤرشفة** — ده مضلل جدًا للأدمن.
- **زرار «استرجاع»:** «حصل خطأ في الاسترجاع».

**الحل:** كتابة الـ ٣ handlers في `doGet`، مع شيت `الأرشيف` جديد:

```javascript
const ARCHIVE_SHEET_NAME = 'الأرشيف';

if (action === 'archiveDone') {
  if (!isAdmin) return denyAdmin_();
  const asheet = getArchiveSheet_();
  const rows = sheet.getDataRange().getValues();
  let n = 0;
  for (let i = rows.length - 1; i >= 1; i--) {
    if (String(rows[i][7]).trim() === STATUS_DONE) {
      asheet.appendRow(rows[i]);
      sheet.deleteRow(i + 1);
      n++;
    }
  }
  syncReservedColumns_();
  return ContentService.createTextOutput(JSON.stringify({ ok: true, archived: n }))
    .setMimeType(ContentService.MimeType.JSON);
}
```
(وبالمثل `archivedOrders` و `restoreArchived`)

---

### م-٢. إشعارات Push مش شغالة خالص

**المكان:** `apps_script.gs` سطر 905–924

```javascript
// بيبعت Web Push بدون payload encryption
// (Chrome/Firefox بيقبلوا JSON مباشرة لو VAPID صح)  ← التعليق ده غلط
const resp = UrlFetchApp.fetch(sub.endpoint, {
  method: 'post',
  headers: { 'Content-Type': 'application/json', 'TTL': '86400' },
  payload: payload
});
```

**المشكلة:** بروتوكول Web Push (RFC 8291 / RFC 8292) بيتطلب حاجتين إجباريتين ومفيش منهم ولا واحدة هنا:
1. **`Authorization: vapid t=<JWT>, k=<publicKey>`** — JWT موقّع بالمفتاح الخاص. مش موجود خالص، فالـ VAPID keys المعرّفة فوق مابتتستخدمش أصلاً.
2. **تشفير الـ payload** بـ `aes128gcm` بمفاتيح `p256dh` و `auth` من الاشتراك. الـ payload هنا JSON خام.

**النتيجة:** خدمة الـ push (FCM/Mozilla) بترد بـ `401 Unauthorized` أو `400 Bad Request` دايمًا. **مفيش ولا إشعار واحد وصل لأي مستخدم من يوم ما الميزة اتكتبت.** والأسوأ إن الكود بيعتبر أي رد ٤٠٤/٤١٠ إنه اشتراك ميت **وبيمسحه** (سطر 921).

وده بيفسّر ليه الاعتماد الأساسي على واتساب في التبليغ.

**الحل:** Apps Script مايقدرش يعمل تشفير `aes128gcm` بشكل عملي. الاختيارات:
- **الأسهل والأنضف:** شيل ميزة الـ push خالص، واعتمد على واتساب (اللي شغال كويس فعلاً)، وشيل معاها `registerPush`, `sendPushSubToServer`, `ensurePushRegistered`, والـ `push` listener في `sw.js`, وعمود K في شيت المستخدمين.
- **لو الإشعارات مهمة:** استخدم خدمة وسيطة (مثلاً Firebase Cloud Messaging HTTP v1 بـ service account، أو OneSignal) — الاتنين بيتنادوا بـ `UrlFetchApp` عادي من Apps Script.

---

### م-٣. أيقونات الإشعارات مسارها غلط

**المكان:** `sw.js` سطر 58–59

```javascript
icon: './icons/icon-192.png',   // ❌ مفيش فولدر اسمه icons
badge: './icons/icon-96.png',   // ❌ الملف ده مش موجود أصلاً
```

الأيقونات في جذر المستودع مباشرة (`icon-192.png`)، ومفيش `icon-96.png` خالص.

**الحل:** `'./icon-192.png'` للاتنين، أو إضافة `icon-96.png` فعليًا.

---

### م-٤. الكتالوج الاحتياطي بيكدب على الموزع

**المكان:** `index.html` سطر 1265 (١٠٩٨ صنف) + سطر 1731

```javascript
CATALOG = FALLBACK_CATALOG.map(d => Object.assign({price:null, available:true, inStock:true}, d));
```

الكتالوج الاحتياطي فيه ٦ حقول بس: `name, main, sub, unit, product, color` — **مفيش أسعار ولا أرصدة**.

فلما النت يقع أو السيرفر يتأخر أكتر من ١٢ ثانية:
- **كل الأصناف بتبان "متاحة"** — حتى الموقوف يدويًا والمنتهي من المخزن.
- **كل الأسعار بتختفي** — الموزع يقدر يبعت طلب من غير ما يشوف سعر.

الموزع ممكن يبعت طلب بأصناف مش موجودة أصلاً، ويستلم رد بعدين إنها كلها مش متاحة.

**الحل:**
1. تخزين آخر كتالوج ناجح في `localStorage` واستخدامه كـ fallback أول (فيه أسعار وأرصدة حقيقية ولو قديمة شوية).
2. لو مفيش غير `FALLBACK_CATALOG`، اعرض شريط تحذير ثابت: «⚠️ الأسعار والأرصدة مش محدّثة — النت مقطوع». الحالة `state.catalogError` موجودة أصلاً بس مش مستغلة كفاية.

---

## 🟡 أخطاء منطقية في البيانات

### ب-١. الحجز بيتحسب مرتين للطلب المعدَّل

**المكان:** `apps_script.gs` سطر 591

```javascript
if (String(r[7] || '').trim() === STATUS_DONE) return;  // بيستثني المنفَّذ بس
```

لما الموزع يعدّل طلب، بيحصل الآتي (حسب `doPostInner_` سطر 125):
- الصف القديم → حالته `مُعدّل`
- صف جديد → حالته `تم استلام الطلب`

والدالة دي بتستثني `تم تنفيذ الطلب` **بس**. يعني الصف `مُعدّل` **لسه بيتحسب كمحجوز**، والصف الجديد بيتحسب كمان.

**النتيجة:** أي طلب يتعدّل بيحجز **ضعف الكمية** من المخزن. ولو اتعدّل تلاتة مرات، بيحجز ٤ أضعاف. الرصيد الظاهر للموزعين بيقل غلط، وأصناف متاحة فعلاً بتبان "مفيش رصيد".

**الحل:**
```javascript
const st = String(r[7] || '').trim();
if (st === STATUS_DONE || st === STATUS_EDITED) return;
```

### ب-٢. الرصيد رقم واحد لكل المخازن، بس الحجز لطنطا بس

الرصيد في شيت الأصناف رقم عام واحد، والحجز بيتطبق على مخزن `طنطا` بس (`RESERVED_WAREHOUSE_`). يعني:
- طلبات الاسكندرية والمنصورة والعاشر **مابتحجزش حاجة**، بس الرصيد اللي بيتخصم منه هو نفسه رقم واحد.
- الموزعين في المخازن دي بيشوفوا كل حاجة "متاحة" دايمًا (`warehouseAffectsStock()` بترجع `false`).

ده اختيار متعمّد وواضح من التعليقات، بس لازم يتراجع: **إما** رصيد منفصل لكل مخزن (أعمدة منفصلة في شيت الأصناف)، **وإما** يتوضّح للأدمن إن رقم الرصيد ده لطنطا بس.

### ب-٣. `deleteDelivered` بيمسح صفوف في حلقة عكسية على شيت مباشر

سطر 1088–1090: الحلقة بتمشي بالعكس (صح ✅) بس كل `deleteRow` نداء منفصل لـ Sheets API. مع ٥٠٠ طلب منفَّذ، ده ٥٠٠ نداء — هيتخطى حد الـ ٦ دقايق بتاع Apps Script ويقع في النص، ويسيب البيانات نص ممسوحة.

**الحل:** تجميع الصفوف المتجاورة ومسحها بـ `deleteRows(start, count)`، أو الاعتماد على الأرشفة (بعد ما تتصلح) بدل الحذف.

---

## 🟡 الأداء وقابلية التوسع

### أ-١. `syncReservedColumns_` بتتنادى في كل عملية

بتتنادى في: حفظ طلب جديد، تعديل، تغيير حالة، حذف، حذف جماعي.

وكل مرة بتعمل:
- `getDataRange().getValues()` على شيت **الأصناف** كامل (١٠٩٨+ صف)
- `getDataRange().getValues()` على شيت **الطلبات** كامل
- `JSON.parse` لكل طلب
- `setValues()` على عمودين × كل صفوف الأصناف

**النتيجة:** إرسال طلب واحد بيقرا الداتابيز كلها مرتين ويكتب ٢٢٠٠ خلية. مع نمو الطلبات ده بيبطّأ خطيًا لحد ما يتعدى حد الـ ٦ دقايق.

**الحل:** تحديث تراكمي (بنضيف/نطرح كميات الطلب اللي اتغيّر بس)، أو تشغيل `syncReservedColumns_` من trigger كل ٥ دقايق بدل ما تكون داخل مسار الحفظ.

### أ-٢. قراءة الشيت كامل في كل نداء

`getDataRange().getValues()` بتتكرر في `handleLogin`, `doPostInner_`, `updateStatus`, `delete`, `lookup`, `listPwRequests_`, `savePushSub_`, `getPushSub_`... وكل واحدة بتحمّل الشيت كله في الذاكرة عشان تدوّر على صف واحد.

**الحل:** `CacheService` للكتالوج (بيتغير نادرًا)، و `TextFinder` للبحث عن صف واحد.

### أ-٣. `index.html` ملف واحد ٦٦٧ كيلوبايت

- سطر **1265**: ٢٣٩ كيلوبايت (الكتالوج الاحتياطي)
- سطر **1266**: ٤١ كيلوبايت (اللوجو base64)

يعني ~٢٨٠ كيلوبايت من الملف بيانات مش كود، وبتتحمّل في كل مرة الملف بيتجدد.

**الحل:** فصلهم لملفين خارجيين (`catalog-fallback.json`, `logo.jpg`) وتحميلهم عند الحاجة بس. ده بيقلل حجم التحميل الأساسي ~٤٢٪.

### أ-٤. `render()` بيعيد بناء الصفحة كلها

`app.innerHTML = ...` بيهدّ ويبني كل DOM في كل تغيير، وبعده `bindEvents()` بيلف على كل `[data-act]` ويربط listeners من جديد.

الشغل الهندسي الموجود عشان يعوّض ده (حفظ التمرير، قفل الارتفاع، `restoreUiState_`) عالي المستوى — بس هو علاج للعرض مش للسبب.

**الحل:** تفويض الأحداث (event delegation) — listener واحد على `#app` بدل مئات الـ listeners:
```javascript
document.getElementById('app').addEventListener('click', e => {
  const el = e.target.closest('[data-act]');
  if (el) { setScrollAnchor(el); onAction(el.dataset.act, el); }
});
```
ده لوحده هيحسّن الاستجابة على الموبايلات الضعيفة بشكل ملحوظ.

### أ-٥. مكتبات خارجية بدون SRI وبدون كاش

سطر 18–20: `exceljs`, `html2canvas`, `jspdf` من `cdnjs.cloudflare.com` — **من غير `integrity` attribute**، ومش موجودين في `PRECACHE` بتاع الـ Service Worker.

**النتيجة:**
1. لو الـ CDN اتخترق، الكود الخبيث هيتنفذ في تطبيقك مباشرة.
2. **التطبيق "أوفلاين" اسميًا بس** — أول ما النت يقطع، PDF والصور و Excel كلهم بيقعوا.

**الحل:** إضافة `integrity` + `crossorigin="anonymous"`، وتنزيل المكتبات محليًا وضمها للـ `PRECACHE`.

---

## 🔵 ملاحظات على الصيانة والجودة

### ص-١. رقم النسخة في ٣ أماكن منفصلة

| الملف | المكان |
|---|---|
| `sw.js` | `CACHE_VERSION = 'v173'` |
| `index.html` | `APP_BUILD = 'v173'` |
| `apps_script.gs` | `version: 'v173'` |

نسيان واحد فيهم = التطبيق يفضل على نسخة قديمة أو التشخيص يدي معلومة غلط. الأخير خطر بالذات لأن `apps_script.gs` بيتنشر يدويًا.

**الحل:** على الأقل تعليق واضح في أول كل ملف، أو سكريبت `bump-version.sh` بسيط.

### ص-٢. `apps_script.gs` نسخة مرجعية بس

الملف ده مش بيتنشر تلقائيًا — لازم يتلصق في محرر Apps Script وينعمله Deploy. **مفيش أي ضمانة إن النسخة المنشورة = النسخة اللي في المستودع.** ودي مصدر أخطاء صامتة (زي ما حصل مع `uploadCatalog` اللي كان زرار في الواجهة والسيرفر مش عارفه — مكتوب في التعليق سطر 805).

**الحل:** استخدام [`clasp`](https://github.com/google/clasp) لنشر Apps Script من المستودع. أو على الأقل استخدام `?action=version` بعد كل Deploy للتأكيد.

### ص-٣. `sw.js` بيعمل reload للصفحة تحت إيد المستخدم

سطر 44–47: عند التفعيل بيعمل `client.navigate(client.url)` لكل التابات المفتوحة. لو الموزع كان بيكتب طلب، الصفحة هتتحدث فجأة.

الحفظ التلقائي للمسودة بيغطي ده جزئيًا ✅، بس المفروض يتأخّر لحد ما المستخدم يكون idle، أو يظهر شريط «فيه تحديث جديد — دوس للتحديث».

### ص-٤. حقول في `state` مش معرّفة في الكائن الأصلي

`state.managePinError` و `state.managePinChecking` بيتستخدموا في `submitManagePin()` (سطر 2400) بس مش موجودين في تعريف `state` (سطر 1357–1489). شغال في JavaScript، بس بيكسر الاتساق وبيصعّب تتبع الحالة.

### ص-٥. مفيش أي اختبارات ولا CI ولا README ولا .gitignore

مستودع فيه ~٨٩٠٠ سطر منطق شغل حقيقي، بصفر اختبارات وصفر أتمتة. تاريخ الكوميتات كله «Add files via upload» — يعني مفيش أي سجل لإيه اتغيّر وليه.

**الحل المقترح (تدريجي، مش دفعة واحدة):**
1. `README.md` بيشرح المعمارية وخطوات النشر.
2. رسايل كوميت واضحة من دلوقتي.
3. اختبارات بسيطة للدوال الحسابية النقية (`computeCartPricing`, `normalizeItemToSmallUnit`, `buildReservedMap_`) — دي أكتر حاجة معرّضة لأخطاء صامتة.

### ص-٦. ملاحظات صغيرة على `manifest.json`

- `start_url: "./index.html"` و `id: "/elkorashy-orders/"` — غير متسقين. الأفضل `start_url: "./"`.
- الـ `PRECACHE` في `sw.js` فيه `'./'` و `'./index.html'` كإدخالين منفصلين — بيخزنوا نفس الملف مرتين.
- `orientation: "portrait"` بيمنع الوضع الأفقي — ممكن يضايق في شاشة متابعة المخزن اللي فيها جدول عريض.

### ص-٧. بوابة التثبيت الإجبارية

`render()` سطر 6666: لو التطبيق مش شغال في وضع standalone، بيعرض شاشة "ثبّت التطبيق" قبل أي حاجة. فيه زرار «متابعة بدون تثبيت» ✅، بس ده حاجز أول انطباع لأي حد بيفتح اللينك من واتساب.

**اقتراح:** خلي البوابة تظهر مرة واحدة، وبعدين شريط `renderInstallBanner()` (اللي موجود أصلاً ومعمول كويس) يكفي.

---

# القسم الثالث: خطة العمل المقترحة 🎯

## المرحلة صفر — النهاردة (أمان، ٢–٣ ساعات)

| # | المهمة | الملف |
|---|---|---|
| ١ | غيّر `ADMIN_PIN` و `MANAGE_PIN` فورًا واعتبر القديم مكشوف | Apps Script Properties |
| ٢ | جدّد `VAPID` keys ومفتاح `CallMeBot` | `apps_script.gs` |
| ٣ | شيل كل الأسرار من `apps_script.gs` وشيل السطر 702 | `apps_script.gs` |
| ٤ | صلّح `lookup`: امنع `endsWith` واشترط ٩ أرقام على الأقل | `apps_script.gs:1107` |
| ٥ | اربط `savePushSub` بتحقق من الهوية | `apps_script.gs:83` |

## المرحلة الأولى — الأسبوع ده (وظائف مكسورة، ١–٢ يوم)

| # | المهمة | الملف |
|---|---|---|
| ٦ | اكتب handlers الأرشيف التلاتة | `apps_script.gs` |
| ٧ | صلّح الحجز المزدوج (استثنِ `STATUS_EDITED`) | `apps_script.gs:591` |
| ٨ | قرار الـ Push: تتشال ولا تتنقل لـ FCM | `apps_script.gs` + `sw.js` |
| ٩ | صلّح مسارات أيقونات الإشعارات | `sw.js:58` |
| ١٠ | كتالوج احتياطي من `localStorage` + شريط تحذير | `index.html:1731` |

## المرحلة الثانية — الشهر ده (أمان أعمق، ٢–٣ أيام)

| # | المهمة |
|---|---|
| ١١ | hash لكلمات المرور + salt لكل مستخدم |
| ١٢ | `login` من GET لـ POST |
| ١٣ | حد لمحاولات الدخول (٥ محاولات → قفل ١٥ دقيقة) |
| ١٤ | توكن جلسة للموزع، و«طلباتي» تبقى بالتوكن مش بالتليفون |
| ١٥ | العمليات المدمِّرة من GET لـ POST |
| ١٦ | `integrity` للمكتبات + ضمّها للـ precache |

## المرحلة الثالثة — لما يبقى فيه وقت (أداء وصيانة)

| # | المهمة |
|---|---|
| ١٧ | `syncReservedColumns_` تراكمية أو على trigger |
| ١٨ | تفويض الأحداث بدل ربط listeners لكل عنصر |
| ١٩ | فصل الكتالوج الاحتياطي واللوجو لملفات مستقلة |
| ٢٠ | `clasp` لنشر Apps Script من المستودع |
| ٢١ | `README.md` + اختبارات للدوال الحسابية |
| ٢٢ | توحيد رقم النسخة في مكان واحد |

---

## كلمة أخيرة

الجزء اللي شدني فعلاً في الكود ده إن كل تعليق فيه بيحكي حكاية مشكلة حقيقية اتحلت — «ده حصل فعلاً واتفقدت طلبات»، «كان بيبان للموزع وكأن التطبيق واقف»، «المتصفح بيسحب صلاحية الضغطة بعد await طويل». ده مستوى فهم للمستخدم ولسلوك المتصفحات مش بيتعلّم من الكتب.

المشاكل اللي فوق مش مشاكل تفكير — كلها من نوع «حاجة اتعملت صح في مكان واتنسيت في مكان تاني»: الـ PIN اتشال من `index.html` وفضل في `apps_script.gs`، وزراير الأرشيف اتعملت في الواجهة والسيرفر اتنسي، والـ `endsWith` اتحطت عشان تسهّل البحث ففتحت الباب على الآخر.

ابدأ بالمرحلة صفر النهاردة — دي اللي بتوقف نزيف حقيقي شغال دلوقتي. الباقي يستنى.
