# CLAUDE.md — ذاكرة المشروع

## ١) تفضيلات المستخدم (مهم — التزم بيها دايمًا)

- **الحديث بالعربي دايمًا** بيني وبين المستخدم (عربي مصري مبسّط زي أسلوب التعليقات في الكود).
- المستخدم بيحب الشرح يكون عملي ومباشر: إيه المشكلة، فين بالظبط (اسم الملف والسطر)، وإيه الحل.
- تعليقات الكود في المشروع مكتوبة بالعربي — أي كود جديد يتكتب بنفس الأسلوب.
- `REVIEW-AR.md` = تقرير الفحص الأصلي (نسخة v173). **معظم اللي فيه اتصلح في v174** — ارجع له للسياق التاريخي بس، مش كقائمة مهام.
- `README.md` = التوثيق الحالي (النشر، الأسرار، الاختبارات).

## ٢) نظرة عامة على المشروع

**الاسم:** القرشي — طلبات الموزعين (نظام طلبات لأبواب وشبابيك الـ UPVC)
**النوع:** تطبيق ويب PWA بصفحة واحدة، من غير أي build step ولا npm ولا framework.
**الاستضافة:** GitHub Pages — `https://abdoseleem83.github.io/elkorashy-orders/`
**المستودع:** `abdoseleem83/elkorashy-orders` — ⚠️ **مستودع عام (public)**، أي حاجة فيه مكشوفة للكل.

### الملفات

| الملف | الحجم | الوصف |
|---|---|---|
| `index.html` | ~396KB / 7807 سطر | التطبيق كله: HTML + CSS + JS في ملف واحد |
| `apps_script.gs` | 1555 سطر | الباك إند (Google Apps Script) — نسخة مرجعية من الكود المنشور |
| `sw.js` | 171 سطر | Service Worker (كاش أوفلاين) |
| `manifest.json` | — | إعدادات الـ PWA |
| `logo.jpg` | 31KB | اللوجو (كان base64 جوه index.html) |
| `data/catalog-fallback.json` | 226KB | أسامي الأصناف — آخر مصدر احتياطي |
| `vendor/` | 1.5MB | html2canvas · jspdf · exceljs — محلية، بتتحمّل عند الطلب بس |
| `tests/flow.mjs` · `tests/admin.mjs` | — | اختبارات بمتصفح حقيقي (Playwright) |
| `icon-192.png` / `icon-512.png` | — | أيقونات التطبيق |

### المعمارية

```
المتصفح (index.html)  →  fetch  →  Google Apps Script /exec  →  Google Sheets
                                          ↓
                                   CallMeBot (واتساب)
```

- مفيش سيرفر ولا داتابيز — Google Sheets هي قاعدة البيانات.
- كل الحالة (state) في كائن `state` واحد، والرسم بـ `app.innerHTML = ...` كامل في `render()`.
- ربط الأحداث بـ `data-act` attribute + **تفويض** من `#app` (`bindEvents()` بتتربط مرة واحدة بس).

### شيتات جوجل المستخدمة

| الشيت | الغرض |
|---|---|
| `Orders` | الطلبات — ١٢ عمود: رقم الطلب، التاريخ، الموزع، المنطقة، الهاتف، ملاحظات، ملخص الأصناف، الحالة، الأصناف (JSON)، المخزن، رقم الطلب بالمخزن، **اسم المستخدم** (ثوابت `COL_*` في `apps_script.gs`) |
| `الأرشيف` | الطلبات المؤرشفة — نفس أعمدة Orders + تاريخ الأرشفة |
| `الأصناف` | الكتالوج — A=الاسم(مفتاح فريد)، B=القطاع، C=فرعي، D=محتوى اللفة، E=المنتج، F=اللون، G=السعر، H=الرصيد، I=موقوف('لا')، + بضاعة محجوزة، رصيد متبقي |
| `المستخدمين` | A=يوزر، B=باسورد، C=اسم الموزع، D=المنطقة، E=الهاتف، F=الحالة، G=التاريخ، H=النوع، J=مفتاح واتساب، K=اشتراك Push |
| `الخصومات` | نسبة الخصم لكل قطاع |
| `طلبات الباسورد` | طلبات تغيير كلمة المرور المنتظرة موافقة |
| `طابور إشعارات واتساب` | طابور الرسائل — trigger كل دقيقة بيبعتها |
| `عداد الطلبات` | عدّاد مستقل لرقم الطلب لكل مخزن |
| `أخطاء` | سجل الأخطاء من الباك إند |

### حاجات مهمة في المعمارية (بعد v174)

- **كل عملية بتقرا = GET، وكل عملية بتغيّر أو بتمسح = POST.** متكسرش القاعدة دي.
- **طبقة الاتصال:** `apiGet(action, params)` و `apiPost(payload)` — استخدمهم، متكتبش `fetch` مباشر. الـ Content-Type لازم يفضل `text/plain` (Apps Script مابيردش على OPTIONS).
- **التوكنات موقّعة بـ HMAC ومش متخزنة** (`signToken_` / `verifyToken_`). ٣ أدوار: `admin` · `manage` · `user`. الأدمن مقبول مكان الإدارة.
- **ربط الأحداث بالتفويض:** `listener` واحد على `#app`، بيتربط **مرة واحدة** في عمر الصفحة. متضيفش `addEventListener` لعناصر فردية جوه الرسم.
- **المودالات:** الخلفية `.modalbg` ليها `data-act="close-x"` واللوحة `data-act="noop"`. `closest()` بيحل المشكلة لوحده — مفيش `stopPropagation` خالص.
- **المكتبات بتتحمّل عند الطلب:** `await ensureLibs_('html2canvas','jspdf')` أو `('exceljs')` قبل أي استخدام. بترجّع `false` لو فشلت (مش بترمي).
- **الأصناف ٣ مصادر بالترتيب:** السيرفر → آخر نسخة ناجحة في `localStorage` → `data/catalog-fallback.json`. الحالتين ٢ و٣ بيظهر معاهم شريط تحذير.

### مفاهيم أساسية

- **حالات الطلب:** `تم استلام الطلب` → `تم تنفيذ الطلب`. وفيه `مُعدّل` للنسخة القديمة من طلب اتعدّل. (حالة `تحت التنفيذ` القديمة بتتحوّل تلقائيًا عند العرض).
- **التعديل:** بيعمل صف **جديد** برقم جديد، والقديم بيتعلّم `مُعدّل` — مش بيحدّث نفس الصف.
- **المخازن:** `طنطا`, `الاسكندرية`, `المنصورة اكسا`, `العاشر اكسا`. الرصيد والحجز بيتطبقوا على **طنطا بس** (`STOCK_TRACKED_WAREHOUSES_` في الواجهة و `RESERVED_WAREHOUSE_` في السيرفر).
- **نوع الحساب:** `موزع` أو `عميل`. العميل مابيتسألش عن المخزن ومابيشوفش حالة الرصيد.
- **الوحدات:** لفة/كرتونة = وحدة كبيرة، بتتحوّل للوحدة الصغيرة (عود/قطعة) بالضرب في عمود D (محتوى اللفة).
- **التوكنات:** `ADMIN_PIN` يفتح متابعة المخزن، `MANAGE_PIN` يفتح تبويب الإدارة. توكن الأدمن مقبول في نداءات الإدارة كمان. التوكن صالح ١٢ ساعة ومتخزن في `sessionStorage`.

## ٣) قواعد تعديل مهمة

1. **رقم النسخة موجود في ٣ أماكن لازم يتغيروا مع بعض:**
   - `sw.js` → `CACHE_VERSION`
   - `index.html` → `APP_BUILD`
   - `apps_script.gs` → `APP_VERSION`
2. `apps_script.gs` في المستودع هو **نسخة مرجعية بس** — التعديل الحقيقي لازم يتلصق في محرر Apps Script ويتعمله Deploy جديد.
3. أي نص من المستخدم بيتحط في HTML لازم يعدّي على `esc()` — الالتزام بده حاليًا كويس جدًا في المشروع، متكسرهوش.
4. مايتحطش أي سر (PIN / API key / private key) في أي ملف في المستودع ده — هو **عام**.
5. المستخدم بيرفع الملفات يدويًا في الغالب (رسايل الكوميت كلها "Add files via upload").

## ٤) اللي اتصلح في v174

كل الحاجات دي كانت مكسورة في v173 واتصلحت واتأكدنا منها باختبار بمتصفح حقيقي:

| المشكلة | الحل |
|---|---|
| `lookup` بيرجّع كل الطلبات بدون تحقق (`endsWith` بدون حد أدنى) | توكن إجباري + مطابقة كاملة + الموزّع يشوف طلباته بس |
| الأسرار مكتوبة في مستودع عام | كلها في Script Properties عن طريق `setupWizard()` |
| كلمات المرور نص صريح | SHA-256 + salt، مع ترقية تلقائية عند أول دخول |
| مفيش حد لمحاولات الدخول | ٥ محاولات → قفل ١٥ دقيقة |
| الأرشيف مكسور (٣ نداءات مش موجودة في السيرفر) | اتكتبوا + شيت `الأرشيف` |
| الحجز بيتحسب مرتين للطلب المعدَّل | `STATUS_EDITED` اتستثنى من `buildReservedMap_` |
| الحذف وتغيير الحالة بـ GET | كلهم بقوا POST |
| `savePushSub` بأي اسم مستخدم | اسم المستخدم بياخده من التوكن |
| Push مكسور من غير ما حد ياخد باله | اتوقف رسميًا واتوثّق (Apps Script مايقدرش يعمله) |
| الكتالوج الاحتياطي بيكدب (كله "متاح" وبدون أسعار) | كاش `localStorage` الأول + تحذير واضح |
| مسارات أيقونات الإشعارات غلط | اتصلحت |
| ١.٥ ميجا مكتبات من CDN معطِّلة للصفحة | محلية + بتتحمّل عند الطلب بس |
| ربط listeners لكل عنصر بعد كل رسم | تفويض — listener واحد مرة واحدة |
| `deleteRow` في حلقة (بيقع مع بيانات كتير) | حذف بالمجموعات المتجاورة |

### لسه مفتوح (مقصود، مش نسيان)

1. **Web Push متوقف** — محتاج خدمة وسيطة (FCM/OneSignal). التفاصيل في README.
2. **الرصيد رقم عام واحد** لكل المخازن والحجز على طنطا بس — قرار شغل، محتاج أعمدة منفصلة لو هيتغير.
3. **`login` لسه بيقبل GET كـ fallback؟** لأ — اتشال. أي عميل قديم هياخد رسالة "حدّث التطبيق".
